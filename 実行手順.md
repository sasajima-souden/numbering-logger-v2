# 新しいPCでのアプリケーション実行手順

このドキュメントは、新しいWindows PCで「毎木調査ナンバリングロガー」を実行するために必要な環境構築とアプリケーションの起動手順を説明します。

---

## 手順

### ステップ1: Node.js のインストール

アプリケーションの実行には、JavaScriptの実行環境である「Node.js」が必要です。

1.  Webブラウザで [Node.js 公式サイト](https://nodejs.org/) にアクセスします。
2.  トップページに表示されている **LTS版**（推奨版）のインストーラーをダウンロードします。
3.  ダウンロードしたインストーラーを実行し、画面の指示に従ってインストールを完了させます。（すべての設定はデフォルトのままで問題ありません。）
4.  インストールが完了したら、PCを再起動してください。

### ステップ2: アプリケーションの配置

開発に使用した `pdf-annotator` フォルダを、USBメモリなどを経由して、新しいPCの分かりやすい場所（例: デスクトップ）にコピーします。

### ステップ3: 依存パッケージのインストール

アプリケーションをビルド（構築）するために必要な、様々な部品（依存パッケージ）をインストールします。

1.  スタートメニューから「コマンドプロンプト」を検索して起動します。
2.  `cd` コマンドを使って、先ほどコピーした `pdf-annotator` フォルダに移動します。
    ```bash
    # 例: デスクトップにフォルダを置いた場合
    cd C:\Users\(あなたのユーザー名)\Desktop\pdf-annotator
    ```
3.  以下のコマンドを実行します。インターネットから必要なファイルがダウンロードされ、`node_modules` というフォルダが作成されます。完了まで数分かかる場合があります。
    ```bash
    npm install
    ```

### ステップ4: アプリケーションサーバーの準備

ビルドしたアプリケーションをローカルPCで起動するための、簡易的なWebサーバー（`serve`）をインストールします。

1.  コマンドプロンプトで、以下のコマンドを実行します。`-g` は、PCのどこからでもこのコマンドを使えるようにするためのオプションです。
    ```bash
    npm install -g serve
    ```

### ステップ5: アプリケーションのビルド

ソースコードから、ブラウザで実行可能な形式のファイル群を生成（ビルド）します。

1.  コマンドプロンプト（`pdf-annotator` フォルダにいる状態）で、以下のコマンドを実行します。
    ```bash
    npm run build
    ```
    これにより、`build` というフォルダが作成されます。これがアプリケーションの本体です。

### ステップ6: アプリケーションの起動

ビルドしたアプリケーションを起動します。

#### 方法A: バッチファイルで起動する（推奨）

`pdf-annotator` フォルダの中に `start_app.bat` というファイルがあれば、それをダブルクリックするだけでサーバーが起動し、自動的にブラウザでアプリケーションが開きます。

**※注意:** `start_app.bat` 内で指定されている `build` フォルダのパスが、PCの環境と異なっている場合は、メモ帳でファイルを開き、パスを修正する必要があります。

#### 方法B: コマンドで起動する

コマンドプロンプトで、以下のコマンドを実行します。

```bash
serve -s build
```

その後、Webブラウザを起動し、アドレスバーに `http://localhost:3000` と入力してアクセスします。
