### Node.jsのインストールとアプリケーションの起動手順

`npm` コマンドが認識されないパソコンでは、まず **Node.js** のインストールが必要です。

#### ステップ1: Node.js のインストール

1.  **公式サイトにアクセス:**
    Webブラウザで Node.js の公式サイトにアクセスします。
    [https://nodejs.org/](https://nodejs.org/)

2.  **インストーラーをダウンロード:**
    トップページに表示されている、**LTS** と書かれた緑色のボタン（推奨版）をクリックして、インストーラーをダウンロードします。

3.  **インストールを実行:**
    ダウンロードしたインストーラーを実行し、画面の指示に従ってインストールを完了させます。（特に設定を変更する必要はなく、基本的に「Next」や「Install」をクリックしていけば大丈夫です。）

#### ステップ2: `serve` パッケージのインストール

Node.js のインストールが完了したら、**新しくコマンドプロンプトを起動し直し**てから、以下のコマンドを実行します。

```bash
npm install -g serve
```

#### ステップ3: アプリケーションの起動

`serve` のインストールが完了したら、アプリケーションの `build` フォルダがある場所でコマンドプロンプトを開き、以下のコマンドを実行します。

**例:** `build` フォルダが `C:\Users\（ユーザー名）\Desktop\build` にある場合

```bash
serve -s "C:\Users\（ユーザー名）\Desktop\build"
```

#### ステップ4: ブラウザでアクセス

コマンドを実行すると、ターミナルに `Serving!` と表示され、`Local:` の右側に `http://localhost:3000` のようなアドレスが表示されます。
Webブラウザでこのアドレスにアクセスすると、アプリケーションが起動します。

---

ご不明な点があれば、いつでもお尋ねください。
